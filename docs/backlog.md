# Deferred Backlog

- Harden BigQuery writes with richer typed merge helpers instead of simple row-level upserts.
- Add richer Salesforce entity resolution beyond name/external-ID matching.
- Add full Google Ads read-only ingestion when credentials and reporting schema are finalized.
- Add Hootsuite API pagination plus stronger CSV fallback validation.
- Add more granular smoke tests that seed and verify a non-production Salesforce sandbox.
